# 配置 pnpm 优先使用 workspace 内的包
prefer-workspace-packages=true

# 配置包名解析策略，避免与外部包冲突
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*

# 配置依赖解析策略
resolution-mode=highest

# 禁用自动安装 peer dependencies 以避免冲突
auto-install-peers=false 